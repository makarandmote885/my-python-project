#types of arguments 
"""
1 positional arguments: the value are matched according to their position
- Because the first value goes to the first parameter, and the second goes to the second 
2 keyword arguments:We explicitly tell Python which parameter gets which value
-Notice the order doesn't matter because we specified the parameter names.
3 default argument :
4 variable length argument 
"""

def student (name,age):
    print(name)
    print(age)
student("makarand",20) 


